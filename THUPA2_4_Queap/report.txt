1.DSA构思：
与MOOC PA2-2Tunel类似，不过是从保存最大值改为保存最小值。
2个栈模拟队列很好想，但如何保存最值，因为对Tunel的实现没有深入理解，
仅仅照方抓药，没有认真领会算法原理，所以这次就被虐了。
还是要认真对待每一道题啊，不仅要通过，还要吃透背后的原理、思想。

2.问题与解决方法：
case10超时，因为使用了list来实现stack。
改为用数组实现后通过。

3.时间复杂度与空间复杂度：
时间：
E:入队操作显然为O(1)
D:由于要将主栈数据转移到辅助栈，不是O(1)；但同一个元素不会被转移多次，因此从分摊意义讲为O(1)
M:数据结构设计的好，极值栈已经保存了最值，直接返回即可，因此O(1)
空间：
Queap使用了2个极值栈模拟队列；
每个极值栈使用2个栈，主栈保存数据，辅助栈保证最值；
每个栈使用了大小为1000001字节的固定数组
O(4n)

4.略

5.略
